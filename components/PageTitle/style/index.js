import styled from 'styled-components';

export const SearchKeyWrapper = styled.div`
  display: flex;
  margin-top: ${({ theme }) => theme.sizes.xxsmall};
`;

export const Container = styled.div`
  display: flex;
  align-items: center;
  justify-content: space-between;
`;
